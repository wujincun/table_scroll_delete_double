/**
 * Created by Administrator on 2016/11/11.
 */
$(function () {
    var $title = $('.title');
    var $content = $('content');
    var $contentLine = $content.find('tr');
    $contentLine.each(function (key,value) {
        var $thisCol = $(value).find('td');
        $thisCol.each(function (key,vule) {
            var thisText = $()
        })

    })
})